package com.evenwell.fqc.baseActivity;

import android.content.Context;
import android.content.res.Configuration;
import android.content.res.Resources;

public class DensityWrapper { /* implementation hidden */ }
    /**
     * 包裝 context，套用指定 density
     */
    public static Context wrap(Context context, int targetDpi) { /* implementation hidden */ }
            // 建立新的 Context，帶入修改後的 configuration
}
