package com.evenwell.fqc.activity;

import com.evenwell.fqc.R;
import com.evenwell.fqc.utility.FQCLog;

import android.content.Context;
import android.media.AudioManager;
import android.util.Log;

public class EarpieceController extends TestItem{ /* implementation hidden */ }
    //private int tempAudioRouting;
    public EarpieceController(Context context) { /* implementation hidden */ }
        //tempAudioRouting = mAudioManager.getRouting(tempAudioMode); //get the current Routing base on current Mode
        //Log.i(TAG, "getRouting 1: " + mAudioManager.getRouting(AudioManager.MODE_NORMAL));
        //Log.i(TAG, "getRouting 2: " + mAudioManager.getRouting(AudioManager.MODE_NORMAL));  //1
    public void setAudioRouting(){ /* implementation hidden */ }
//        mAudioManager.setMode(AudioManager.MODE_NORMAL);
        //mAudioManager.setRouting(mAudioManager.getMode(), AudioManager.ROUTE_EARPIECE, AudioManager.ROUTE_ALL);
    //below's method don't use now
}


        //mAudioManager.setRouting(tempAudioMode, tempAudioRouting, AudioManager.ROUTE_ALL);

    @Override
    public String getTestItemText() { /* implementation hidden */ }
        // TODO Auto-generated method stub
}

