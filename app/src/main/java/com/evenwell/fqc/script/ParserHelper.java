package com.evenwell.fqc.script;

import android.content.SharedPreferences;

import android.text.TextUtils;
import android.util.Log;

public class ParserHelper { /* implementation hidden */ }
    public ParserHelper(SharedPreferences.Editor e) { /* implementation hidden */ }
    public void open(String t) { /* implementation hidden */ }
        //Log.d(TAG, "open(), tag = " + tag);
    public void close() { /* implementation hidden */ }
            //Log.d(TAG, "close(), put " + tag + " = " + sb.toString());
}

    public String append(String str) { /* implementation hidden */ }
            //Log.d(TAG, "append <- " + str);
            //Log.d(TAG, "append -> " + str);
                //Log.d(TAG, "append(), " + sb.toString());
}
