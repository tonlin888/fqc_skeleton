package com.evenwell.fqc.script;

import android.text.TextUtils;
import android.util.Log;
import java.util.ArrayList;
import java.util.LinkedHashMap;

public class CommandQueue { /* implementation hidden */ }
    public class Script { /* implementation hidden */ }
        public Script(String script) { /* implementation hidden */ }
            //Log.d(TAG, "Script(), script = " + script);
                    //Log.d(TAG, "Script(), " + begin + "-" + end + " => " + script.substring(begin, end).replace("\\;", ";").trim());
}
        public String readLine() { /* implementation hidden */ }
}
        public void next() { /* implementation hidden */ }
}

    public CommandQueue(String data) { /* implementation hidden */ }
}

    public Command next() { /* implementation hidden */ }
        // skip comment
}

        // Pseudo Command

            //Log.d(TAG, "next, mScript.readLine() = " + mScript.readLine());
        //Log.d(TAG, "next, ret = " + ret);

    public boolean gotoLabel(String label) { /* implementation hidden */ }
}
